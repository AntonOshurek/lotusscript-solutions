<p>Wystąpił błąd podczas zapisywania zmian.</p>

<p>Zmiany nie zostały zapisane. Spróbuj ponownie za chwilę.</p>

<p>Jeżeli problem będzie się powtarzał, skontaktuj się z administratorem systemu.</p>

<hr>

<p><strong>Informacje techniczne</strong></p>
<ul>
  <li>Kod błędu: <strong>ROW_EDIT_SUBMIT_FAILED</strong></li>
  <li>Target: <code>${payload.targetMetadata.targetCode}</code></li>
  <li>Kolumna klucza: <code>${payload.targetMetadata.keyColumn}</code></li>
  <li>Kolumna wersji: <code>${payload.targetMetadata.versionColumn}</code></li>
  <li>Szczegóły: <code>${uiError.title}</code></li>
</ul>
