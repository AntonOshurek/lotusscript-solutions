<p>Nie udało się zweryfikować konfiguracji edycji tego rekordu.</p>

<p>Konfiguracja otrzymana z serwera jest niezgodna z danymi wyświetlanymi w tabeli, dlatego zapis został przerwany.</p>

<p>Odśwież stronę i spróbuj ponownie. Jeżeli problem będzie się powtarzał, skontaktuj się z administratorem systemu.</p>

<hr>

<p><strong>Informacje techniczne</strong></p>
<ul>
  <li>Kod błędu: <strong>ROW_EDIT_METADATA_COLUMNS_NOT_FOUND</strong></li>
  <li>Kolumna klucza: <code>${keyColumn}</code></li>
  <li>Kolumna wersji: <code>${versionColumn}</code></li>
</ul>




<p>Nie udało się przygotować danych do zapisania.</p>

<p>Otrzymano nieprawidłowe dane wymagane do identyfikacji rekordu lub jego wersji, dlatego zapis nie może zostać wykonany.</p>

<p>Odśwież stronę i spróbuj ponownie. Jeżeli problem będzie się powtarzał, skontaktuj się z administratorem systemu.</p>

<hr>

<p><strong>Informacje techniczne</strong></p>
<ul>
  <li>Kod błędu: <strong>ROW_EDIT_METADATA_INVALID_VALUES</strong></li>
  <li>Kolumna klucza: <code>${keyColumn}</code></li>
  <li>Kolumna wersji: <code>${versionColumn}</code></li>
  <li>Wymagany typ: <code>number</code></li>
</ul>
